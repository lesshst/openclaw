# 源码版本升级到最新

## 1. 目标

将 canonical 源码：

- `/Users/lizhibo/.openclaw/runtime/openclaw-src-live`

升级到 upstream 最新状态，同时保留现场已有的本地修复，并把结果推送到：

- `git@github.com:lesshst/openclaw.git`

## 2. 升级前状态

升级前本地源码位于：

- 分支：`main`
- 头提交：`d1db15d40`

同时工作区内还保留了一批现场修复，主要涉及：

- gateway 启动脚本
- agent workspace 路径修复
- OpenAI Codex 多账号回退修复
- 全局代理 `fetch` 修复
- WhatsApp inbound/LID 兼容修复

如果直接拉最新并覆盖，存在把这些本地修复冲掉的风险。

## 3. 实际处理过程

### 3.1 获取 upstream 最新代码

远端配置：

- `origin -> git@github.com:lesshst/openclaw.git`
- `upstream -> https://github.com/openclaw/openclaw.git`

执行：

- `git fetch --all --prune`

拉取后，`upstream/main` 最新头提交为：

- `b7ca9082e`

### 3.2 先保护本地未提交修改

为避免升级时丢失现场修复，先执行 stash，包含未跟踪文件：

- `git stash push --include-untracked -m "codex-pre-upgrade-20260314-0742"`

### 3.3 合并 upstream/main

执行：

- `git merge --no-edit upstream/main`

本次实际出现冲突并需要手工处理的点是：

- `/Users/lizhibo/.openclaw/runtime/openclaw-src-live/.gitignore`

处理原则：

- 保留 upstream 新增的忽略规则
- 同时保留本地 runtime 现场所需的忽略规则

完成后形成升级合并提交：

- `6c0dd139f`
- `Merge remote-tracking branch 'upstream/main'`

### 3.4 恢复现场本地修复

执行：

- `git stash pop`

恢复过程中出现冲突的文件有：

- `/Users/lizhibo/.openclaw/runtime/openclaw-src-live/src/agents/auth-profiles/oauth.openai-codex-refresh-fallback.test.ts`
- `/Users/lizhibo/.openclaw/runtime/openclaw-src-live/src/infra/net/undici-global-dispatcher.ts`

处理方式：

- 保留 upstream 新结构和新接口
- 将本地对多账号回退和全局代理分发的修复重新叠加进去

同时，为适配 upstream 本轮依赖和实现变化，又补了两个兼容修复：

- `/Users/lizhibo/.openclaw/runtime/openclaw-src-live/src/infra/net/undici-global-dispatcher.test.ts`
- `/Users/lizhibo/.openclaw/runtime/openclaw-src-live/src/web/monitor-inbox.test-harness.ts`

## 4. 验证过程

### 4.1 依赖同步

执行：

- `pnpm install --frozen-lockfile`

确保 lockfile 和当前上游依赖树一致。

### 4.2 测试验证

执行：

- `pnpm vitest run src/agents/auth-profiles/oauth.openai-codex-refresh-fallback.test.ts src/agents/auth-profiles/oauth.test.ts src/agents/agent-scope.test.ts src/infra/net/undici-global-dispatcher.test.ts src/web/inbound/access-control.test.ts src/web/monitor-inbox.streams-inbound-messages.test.ts`

结果：

- `65 passed`

### 4.3 构建验证

执行：

- `pnpm build`

结果：

- 构建通过
- A2UI bundle 正常复用/生成

## 5. 提交与推送

在升级合并和本地修复都整理完成后，创建本地提交：

- `fe5b52226`
- `chore: merge upstream and preserve local runtime fixes`

随后推送到 GitHub：

- `origin/main`

推送结果：

- `d1db15d40..fe5b52226  main -> main`

这意味着：

- 本地 canonical 源码已升级
- GitHub 上的 `lesshst/openclaw` `main` 也已同步到同一状态

## 6. 运行态对齐

为确保“源码头提交”和“实际运行的 gateway”一致，升级完成后又重新执行：

- `pnpm build`
- `bash scripts/gw-restart.sh`

复查结果：

- LaunchAgent：`ai.openclaw.gateway-src`
- 运行 PID：`45067`
- 工作目录：
  - `/Users/lizhibo/.openclaw/runtime/openclaw-src-live`
- 监听端口：
  - `127.0.0.1:18789`
  - `[::1]:18789`

版本复查：

- `node dist/index.js --version`
- 输出：`OpenClaw 2026.3.13 (fe5b522)`

通道健康复查：

- `node dist/index.js channels status --probe`

结果：

- `WhatsApp default: enabled, configured, linked, running, connected`
- `Discord default: enabled, configured, running, connected`
- `Feishu default: enabled, configured, running, works`

## 7. 本次升级涉及的关键文件

- `/Users/lizhibo/.openclaw/runtime/openclaw-src-live/.gitignore`
- `/Users/lizhibo/.openclaw/runtime/openclaw-src-live/scripts/gw-common.sh`
- `/Users/lizhibo/.openclaw/runtime/openclaw-src-live/src/agents/agent-scope.ts`
- `/Users/lizhibo/.openclaw/runtime/openclaw-src-live/src/agents/agent-scope.test.ts`
- `/Users/lizhibo/.openclaw/runtime/openclaw-src-live/src/agents/auth-profiles/oauth.ts`
- `/Users/lizhibo/.openclaw/runtime/openclaw-src-live/src/agents/auth-profiles/oauth.openai-codex-refresh-fallback.test.ts`
- `/Users/lizhibo/.openclaw/runtime/openclaw-src-live/src/index.ts`
- `/Users/lizhibo/.openclaw/runtime/openclaw-src-live/src/infra/net/undici-global-dispatcher.ts`
- `/Users/lizhibo/.openclaw/runtime/openclaw-src-live/src/infra/net/undici-global-dispatcher.test.ts`
- `/Users/lizhibo/.openclaw/runtime/openclaw-src-live/src/web/inbound/access-control.ts`
- `/Users/lizhibo/.openclaw/runtime/openclaw-src-live/src/web/inbound/monitor.ts`
- `/Users/lizhibo/.openclaw/runtime/openclaw-src-live/src/web/monitor-inbox.streams-inbound-messages.test.ts`
- `/Users/lizhibo/.openclaw/runtime/openclaw-src-live/src/web/monitor-inbox.test-harness.ts`

## 8. 最终结论

这次不是单纯执行一次 `git pull`，而是完成了一次带现场修复保留的源码升级闭环：

1. 已合并 upstream 最新代码
2. 已解决升级冲突
3. 已恢复并兼容本地关键修复
4. 已完成测试和构建验证
5. 已推送到 GitHub `origin/main`
6. 已确认运行中的 gateway 与最新源码一致

当前可作为唯一准源码继续使用的仍然是：

- `/Users/lizhibo/.openclaw/runtime/openclaw-src-live`
